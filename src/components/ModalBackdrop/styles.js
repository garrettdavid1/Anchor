export const styles = {
    backgroundColor: 'black',
    position: 'fixed',
    top: 0,
    left: 0,
    opacity: .8,
    width: window.innerWidth + 'px',
    height: window.innerHeight + 'px',
    zIndex: 999
}